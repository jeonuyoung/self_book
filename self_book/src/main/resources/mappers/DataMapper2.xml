<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seeyou.cu.DAO.DataMapper2">  <!-- namespace로 Mapper를 확인한다. -->

	<!-- get html-->
	<insert id="inserthtml" parameterType="String" >
		insert into data
		values(#{0}, #{1})
	</insert>
	
	
		<!-- get html-->
	<insert id="makefile" parameterType="String" >
		insert into file
		values(#{0}, #{1})
	</insert>
	

<!-- 
	<select id="gethtml" parameterType="String">
		select html from data where id="coolpark93@gmail.com"
	</select>
 -->

</mapper>

